# headify will examine all the C files in the current directory, matching
# function declatations at top level, and print each to standard out as
# function prototypes.
#
# to-do: echo to file, accept name of file to make, accept list of includes
# end goal: make just -> make re -> make fclean; make all
#     -> headify "libft.h" "<stdlib.h>" "<string.h>" "unistd.h"
#     -> (a perfectly normal 42-normed header file)
#   -> $(CC) -I$(header) ...

find *.c | xargs egrep -h "^(int|char|size_t|void|long).*)$" | sed '/$/s/$/;/'
